---
title: Age Calculator
description: Age Calculator is a Hybrid mobile app developed using Cordova. Boostrap framework for UI development.
keywords: [age, calculator, 'android hybrid application', 'age calculator by shivaji', 'age calculator android application', 'cordova hybrid android app']
date: 2016-01-17T00:00:00.000Z
---

<div class="row">
  <div class="col-md-12">
    <goat-text expressive>Age Calculator is a Hybrid mobile app developed using <a href="https://cordova.apache.org" title="Cordova">Cordova</a>. <a href="https://getbootstrap.com" title="Bootstrap">Boostrap</a> framework for UI development.</goat-text>
    <a href="https://play.google.com/store/apps/details?id=codemons.age_calculator" target="_blank" class="btn btn-primary margin-right-10px">Play Store</a>
    <a href="https://play.google.com/store/apps/details?id=codemons.age_calculator" target="_blank" class="btn btn-primary"><i class="fa fa-github"></i></a>

    <h5>Version</h5>
    <p>1.6.0</p>

    <h5>Dev Setup</h5>
    ```bash
$ npm install -g cordova
```

    ```bash
$ git clone https://github.com/codemons/age-calculator-cordova.git age-calculator
$ cd age-calculator
$ cordova platform add [android/ios/windows]
```

    <goat-text expressive>Install platform respective SDK. And then give build.</goat-text>
    ```bash
$ cordova build
```
  </div>
</div>

<div class="row">
  <div class="col-md-12">
    <h5>Code</h5>
    ```javascript
var getAge = (function(){
  function difference(d1, d2) {
    var m = moment(d1);
    var years = m.diff(d2, 'years');
    m.add(-years, 'years');
    var months = m.diff(d2, 'months');
    m.add(-months, 'months');
    var days = m.diff(d2, 'days');

  return {years: years, months: months, days: days,
    toString: function(){
      var age= [];
      if(this.years > 0) age.push(this.years+ ' year'+(this.years> 1? 's ':' '));
      if(this.months > 0) age.push(months+ ' month'+(this.months> 1? 's':''));
      if(this.days > 0) age.push(this.days+ ' day'+(this.days> 1? 's':''));
      if(age.length > 1)
      age.splice(age.length-1,0,' and ');
      return age.join('');
    }
  };
  }

  return function(dob){
  return difference(new Date(), dob);
  };
}());
```

    <h5>Screenshots</h5>
    <div class="row">
      <div class="col-md-3">
        <img src="/assets/img/products/age-calculator/1.png" width="100%" class="materialboxed padding-top-10px" />
      </div>
      <div class="col-md-3">
        <img src="/assets/img/products/age-calculator/2.png" width="100%" class="materialboxed padding-top-10px" />
      </div>
      <div class="col-md-3">
        <img src="/assets/img/products/age-calculator/3.png" width="100%" class="materialboxed padding-top-10px" />
      </div>
      <div class="col-md-3">
        <img src="/assets/img/products/age-calculator/4.png" width="100%" class="materialboxed padding-top-10px" />
      </div>
    </div>
  </div>
</div>

<hr/>
<goat-text expressive>Free Software, Hell Yeah!</goat-text>
