Install ruby & ruby dev kit

```bash
gem install jekyll
jekyll new .
git config --add core.worktree ../deploy/
```

```bash
bundle install
```

```bash
gem "jekyll-minifier"
gem "jekyll-contentblocks"
gem "jekyll-sitemap"
```
