import { ComponentInterface } from '../../../stencil-public-runtime';
/**
 * @name Notification Manager
 * @description Manages alert, toasts and notifications.
 * @img /assets/img/notification-manager.png
 */
export declare class NotificationManager implements ComponentInterface {
  name: string;
  position: 'top-right' | 'top-left' | 'bottom-right' | 'bottom-left';
  elm: HTMLElement;
  notifications: any;
  isDarkMode: boolean;
  listenToast(evt: any): void;
  listenNotification(evt: any): void;
  componentWillLoad(): void;
  renderNotification(notification: any): any;
  render(): any;
}
