import { ComponentInterface, EventEmitter } from '../../../stencil-public-runtime';
/**
 * @name Notification
 * @description Notifications are messages that communicate information to the user.
 * @example <goat-notification state="success" low-contrast dismissible>
 *               <div slot='title'>Successful saved the record</div>
 *             </goat-notification>
 */
export declare class Notification implements ComponentInterface {
  elm: HTMLElement;
  state: 'success' | 'error' | 'info' | 'warning';
  lowContrast: boolean;
  dismissible: boolean;
  actionable: boolean;
  actionName: string;
  actionLabel: string;
  goatDismiss: EventEmitter;
  hidden: boolean;
  isDarkMode: boolean;
  componentWillLoad(): void;
  render(): any;
  renderAction(): any;
  renderCloseButton(): any;
  renderStateIcon(): any;
}
