import { ComponentInterface, EventEmitter } from '../../../stencil-public-runtime';
/**
 * @name Code Editor
 * @description A browser based code editor.
 * @img /assets/img/code-editor.png
 */
export declare class CodeEditor implements ComponentInterface, InputComponentInterface {
  gid: string;
  /**
   * The input field name.
   */
  name: string;
  /**
   * The input field value.
   */
  value: string;
  /**
   * If true, required icon is show. Defaults to `false`.
   */
  required: boolean;
  /**
   * If true, the user cannot interact with the button. Defaults to `false`.
   */
  disabled: boolean;
  readonly: boolean;
  theme: 'vs-light' | 'vs-dark';
  language: 'javascript' | 'json' | 'html';
  lineNumbers: 'off' | 'on';
  /**
   * Emitted when the value has changed..
   */
  p4Change: EventEmitter;
  /**
   * Set the amount of time, in milliseconds, to wait to trigger the `onChange` event after each keystroke.
   */
  debounce: number;
  hasFocus: boolean;
  protected debounceChanged(): void;
  disabledWatcher(newValue: string): void;
  readonlyWatcher(newValue: string): void;
  languageWatcher(newValue: string): void;
  themeWatcher(newValue: string): void;
  valueWatcher(newValue: string): void;
  getComponentId(): Promise<string>;
  /**
   * Sets focus on the native `input` in `goat-input`. Use this method instead of the global
   * `input.focus()`.
   */
  setFocus(): Promise<void>;
  /**
   * Sets blur on the native `input` in `goat-input`. Use this method instead of the global
   * `input.blur()`.
   */
  setBlur(): Promise<void>;
  private editorElement?;
  editorMonacoInstance: any;
  componentWillLoad(): Promise<void>;
  componentDidLoad(): void;
  private initializeMonaco;
  render(): any;
}
