import { Component, h } from '@stencil/core';
/**
 * @name Header
 * @description Headers are compositions that extend standard navbar functionalities.
 * @img /assets/img/header.png
 */
export class Header {
  renderLeftSection() {
    return (h("div", { class: 'left-section section' },
      h("slot", { name: 'left-section' })));
  }
  renderRightSection() {
    return (h("div", { class: 'right-section section' },
      h("slot", { name: 'right-section' })));
  }
  render() {
    return (h("header", { class: { 'header': true } },
      h("div", { class: 'header-content' },
        this.renderLeftSection(),
        this.renderRightSection())));
  }
  static get is() { return "goat-header"; }
  static get encapsulation() { return "shadow"; }
  static get originalStyleUrls() { return {
    "$": ["header.scss"]
  }; }
  static get styleUrls() { return {
    "$": ["header.css"]
  }; }
}
//# sourceMappingURL=header.js.map